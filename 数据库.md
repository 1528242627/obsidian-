# 数据库
## 1.三级模式-两级映射
外模式对应用户看到的东西，对应数据库中的视图级别，比如select，可以有多种
概念模式（逻辑模式）：表级别（关系），1种情况（只能是表）
内模式：1种情况（物理文件）

外模式—（逻辑独立性/模式映像）—概念模式—（物理独立性/内模式映射）—内模式，两两之间相互映射

## 2.数据库设计
分为规划、需求分析、概念设计、逻辑设计和物理设计五个阶段。

阶段	做什么	产出物	是否关心具体数据库
需求分析	收集用户数据需求	数据流图、数据字典	不关心
概念设计	抽象出业务里"有什么实体、实体有什么关系"	E-R图	完全不关心，是DBMS无关的
逻辑设计	把E-R图转换成某种数据模型能实现的结构	关系模式（表结构定义，也可以是非关系型模型，包括键值存储、文档存储、图形存储等）	关心数据模型（关系/层次/网状），但不关心具体哪个DBMS产品
物理设计	定索引、存储方式、分区	存储结构、存取路径	关心具体DBMS（MySQL/PostgreSQL等）


逻辑设计的产出：不是ER图本身，而是关系模式——用关系模式表示法写出来的表结构。

关系模式是对关系数据库中一张表结构的完整描述，形式化表示为 R(U, D, DOM, F)，其中包含关系名、属性集合、属性所属域、属性到域的映射以及属性间的函数依赖。
关系模型中的一行是一个元组。

### 2.1 ER图
ER模型可转换为关系模型，其中每个实体对应一个关系模式。

带两条竖杠的实体称为子实体，它是实体的某种特例。
弱实体：依赖于强实体（正常实体）而存在。比如学生和紧急联系人之间的关系。是两个长方形框在一起的，也就是一个回形。
联系：用菱形表示。一对一、一对多和多对多三种情况。
属性：用椭圆表示。

### 2.2 关系代数（表之间的运算）（笛卡尔集、投影、选择，以及差集）
笛卡尔积（X）：m*n条，产生的结果包括m和n的所有属性列，最终产出m乘n个记录数，m加n个属性列。
自然连接（像是一个蝴蝶结的符号）：重复的列只显示一次，仅显示两个关系模式中属性相同且值相同的记录。且优先显示第一张表的属性。

投影（π，选属性，Name）：实际是按条件选择某关系模式中的某列，列也可以用数字表示。
从第一张表到第二张表的属性依次用1到n表示。

选择（σ，选记录,No）：实际是按条件选择某关系模式中的某条记录，可以选择，比如1=4，即第一个表的第1个属性等于 将两个表属性相加的第4个属性。

### 2.3 sql语句（from默认采用的是笛卡尔积）
Group后面只能跟having，不能跟where

### 2.4 范式和函数依赖
部分函数依赖（主键有多个）：主键是（A,B），可确定C。单独一个A也可确定C。
传递依赖：若A和B不等价时，A可确定B，B可确定C，则A可确定C。
候选键（能唯一标识此表的组合，去掉冗余的属性）
主属性（候选键内的属性都是主属性）


1NF第一范式：每个属性都是不可分的。
2NF：消除了非主属性对于主属性的部分函数依赖。
3NF：消除了非主属性对于主属性的传递函数依赖。
BCNF：消除了主属性之间的部分函数依赖和传递函数依赖。
4NF：消除非平凡的多值依赖。





